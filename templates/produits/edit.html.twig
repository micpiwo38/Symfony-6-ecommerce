{% extends 'base.html.twig' %}

{% block title %}Edit Produits{% endblock %}

{% block body %}
    <div class="container w-50 bg-white p-3 mt-3 rounded shadow">
        <h1 class="text-danger">Editer le produits</h1>

        {{ include('produits/_form.html.twig', {'button_label': 'Mettre a jour'}) }}

        <a class="btn btn-warning mt-3" href="{{ path('app_produits_index') }}">Annuler</a>

        {{ include('produits/_delete_form.html.twig') }}
    </div>

{% endblock %}
